{
  "jsonSchema": {
    "type": "object",
    "title": "Comment",
    "properties": {
      "nokey": {
        "type": "object",
        "title": "Optional Object without key",
        "properties": {
          "name": {
            "title": "Name",
            "type": "string"
          }
        },
        "required": ["name"]
      },
      "entity": {
        "type": "object",
        "title": "Optional Object",
        "properties": {
          "displayed": {
            "title": "add optional object",
            "type": "string"
          },
          "name": {
            "title": "Name",
            "type": "string"
          }
        },
        "required": ["name"]
      },
      "requiredEntity": {
        "type": "object",
        "title": "Required Object",
        "properties": {
          "name": {
            "title": "Name",
            "type": "string"
          }
        },
        "required": ["name"]
      }
    },
    "required": ["requiredEntity"]
  },
  "uiSchema": [
    {
      "widget": "checkbox",
      "key": "entity"
    },
    {
      "widget": "fieldset",
      "title": "Optional object",
      "condition": {
        "!!": [{ "var": "entity"}]
      },
      "items": [
        {
          "key": "entity.name",
          "description": "Optional object should let me do not display this field"
        }
      ]
    },
    {
      "widget": "fieldset",
      "title": "Entity without key",
      "items": [
        {
          "key": "nokey.name",
          "description": "Optional object should let me do not display this field"
        }
      ]
    }
  ]
}
